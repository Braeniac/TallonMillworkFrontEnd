
const INITIAL_STATE = {
    report: null,
    reportError: ''
}

export default auth = (state=INITIAL_STATE, action) => {
    switch(action.type) {

        default:
            return state; 
    }

}